// requires..

// to build the page content
module.exports.buildPage = function buildPage (pageId){
	var fs =  Platform.fs;
	var modelUser = JSON.parse(fs.readFileSync("model_user.json"));
	
	return "";
}

